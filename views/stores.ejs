<h1>Stores</h1>
<a href="/add-store">Add Store</a>

<table style="border-collapse: separate; border-spacing: 5px; border: 1px solid black;"></table>
  <thead>
    <tr>
      <th>SID</th>
      <th>Location</th>
      <th>MANAGER ID</th>
      <th>Action</th>
    </tr>
  </thead>
  <tbody>
    <% for(var i=0; i<stores.length; i++) {%>
    <tr>
      <td><%= stores[i].sid %></td>
      <td><%= stores[i].location %></td>
      <td><%= stores[i].manager_id %></td>
      <td>
        <a href="/edit-store/<%= stores[i].sid %>">Update</a>
      </td>
    </tr>
    <% } %>
  </tbody>
</table>

<a href="/">Home</a>
